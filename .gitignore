# ───────── Elixir / Mix / Erlang ─────────
/_build/
/deps/
*.beam
*.ez
erl_crash.dump
.erlang.cookie

# Dialyzer PLTs (often large, machine-local)
*.plt
*.plt.hash
priv/plts/
apps/*/priv/plts/

# ───────── Logs, coverage, temp ─────────
/log/
tmp/
cover/
*.coverdata

# ───────── Editor / Tools ─────────
.elixir_ls/
apps/*/.elixir_ls/
.elixir-tools/
.elixir-tools.json
/.idea/
/.vscode/
*.swp
*~
# Mac / Windows cruft
.DS_Store
Thumbs.db

# ───────── Phoenix build outputs ─────────
# (esbuild/vite emit to priv/static/assets)
apps/*/priv/static/assets/
apps/*/priv/static/cache_manifest.json

# ───────── Node (umbrella + per-app) ─────────
/assets/node_modules/
apps/*/assets/node_modules/
# If any app uses Vite/other tooling caches:
apps/*/assets/.vite/
apps/*/assets/.cache/
apps/*/assets/dist/

# ───────── Env / secrets ─────────
.env
.env.*
config/*.secret.exs
config/*_secret.exs
config/prod.secret.exs

# ───────── Project-specific ─────────
# NegCache DETS store (runtime cache)
apps/core/priv/negcache/
*.dets

# Large local data you don't want in Git
apps/db/priv/kaikki.org-dictionary-English.jsonl

apps/db/priv/kaikki.org-dictionary-English.jsonl
# Massive Kaikki/Wiktextract dump (generated/local-only)
apps/db/priv/english.jsonl

