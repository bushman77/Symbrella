defmodule Brain.BG do
  @moduledoc """
  Basal Ganglia (gating/credit assignment placeholder).
  Will later gate working-memory updates and route reinforcement signals.
  """
  use Brain.Region, region: :bg
end

