# Contributing to Symbrella

Thanks for taking a look! This project uses an umbrella layout with clear app boundaries
(e.g. `apps/brain`, `apps/core`, `apps/symbrella_web`). Keep changes focused and small.

## Branch & PR

- Branch names:
  - Features: `feat/<scope>`
  - Fixes: `fix/<scope>`
  - Docs: `docs/<scope>`
- Target small PRs (≤ ~300 LOC preferred).
- When a change touches guardrails or is riskier than normal, explicitly authorize it in the PR with:

